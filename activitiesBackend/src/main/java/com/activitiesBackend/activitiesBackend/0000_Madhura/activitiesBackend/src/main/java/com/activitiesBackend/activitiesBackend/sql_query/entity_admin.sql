CREATE TABLE `activitiesdb`.`hod` (
  `hod_id` INT NOT NULL,
  `hod_name` VARCHAR(45) NULL,
  `department` VARCHAR(45) NULL,
  `phone` VARCHAR(45) NULL,
  `address` VARCHAR(45) NULL,
  `bdate` VARCHAR(45) NULL);